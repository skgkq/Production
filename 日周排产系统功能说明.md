# 日/周排产系统 · 功能说明文档

> **系统名称**：PRODUCTION PLANNING · 一号线  
> **系统定位**：面向制造业的日/周生产排产调度系统，支持多型号、多批次、多工序的自动化排产计划生成。  
> **当前版本**：v4.1（值班表驱动人员约束、按工作日配置、HGNN 同步、插单工单号稳定）

---

## 目录

1. [系统概览](#1-系统概览)
2. [本周计划](#2-本周计划)
3. [型号配置](#3-型号配置)
4. [约束参数](#4-约束参数)
5. [排产结果](#5-排产结果)
6. [调度算法](#6-调度算法)
7. [动态重调度](#7-动态重调度)
8. [智能客服助手](#8-智能客服助手)
9. [附：系统设备清单](#9-附系统设备清单)
10. [附：部署与运行](#10-附部署与运行)

---

## 1. 系统概览

系统顶部导航栏实时显示当前排产任务的关键摘要信息：

| 指标 | 说明 | 示例值 |
|------|------|--------|
| 批次数 | 本次排产的总批次 | 2 批 |
| 型号数 | 涉及的产品型号种数 | 3 型号 |
| 计划天数 | 本周排产覆盖的工作天数 | 5 天 |

### 1.1 顶部操作

| 功能 | 说明 |
|------|------|
| **调度算法** | 下拉切换「贪心算法」或「HGNN+PPO 智能调度」 |
| **▶ 生成排产** | 按当前计划与约束计算排产，成功后跳转「排产结果」 |
| **Tab 导航** | 本周计划 · 型号配置 · 约束参数 · 排产结果（有结果时显示批次数角标） |

切换算法、修改计划/型号/约束后，已有排产结果会清空，需重新生成。

---

## 2. 本周计划

**入口**：顶部 Tab → 「本周计划」

本模块用于录入本周需要生产的任务清单，是排产系统的数据输入起点。

### 2.1 任务列表

每条生产任务包含以下字段：

| 字段 | 说明 |
|------|------|
| **优先级** | 数字越小优先级越高，支持 ＋/－ 调整；高优先级任务优先排入计划 |
| **型号** | 产品型号，从下拉列表中选择（如 A型、B型、C型），对应不同工艺参数 |
| **批次** | 该型号本次需生产的锅数，支持 ＋/－ 调整 |
| **备注** | 可选的自由文本备注字段 |

每条任务下方以标签形式展示该型号各工序的预计工时，例如：

```
称量 0h30m  |  预混 1h  |  混合 3h  |  模具装配 1h  |  成型 4h  |  整装 1h
```

### 2.2 操作功能

- **新增任务**：点击右上角「+ 新增任务」按钮添加新的生产型号任务
- **删除任务**：点击任务行右侧红色「✕」按钮删除该条任务
- **优先级 / 批次调整**：通过 ＋/－ 按钮修改

### 2.3 计划汇总面板（右侧）

| 指标 | 说明 |
|------|------|
| 型号数 | 当前任务涉及的型号种类数量 |
| 总批次 | 所有任务的总锅数之和 |
| 排产天 | 计划覆盖的工作日天数 |
| 班次工时 | 每日机器生产时间段（如 8:00–18:00） |

各型号任务以色块列表形式呈现，显示优先级与批次数。

### 2.4 操作提示

系统内置操作引导：

1. 调整优先级 → 高优先先排  
2. 在「型号配置」修改工艺参数  
3. 在「约束参数」设置值班表 / 库存与班次  
4. 选择算法后点击「生成排产」  
5. 排产后可使用「动态重排」处理插单 / 设备故障  

---

## 3. 型号配置

**入口**：顶部 Tab → 「型号配置」

本模块用于配置各产品型号的工艺参数，系统排产时依据此处数据自动计算各工序工时。

### 3.1 型号工序表

每个型号独立配置一张工序参数表，表头字段如下：

| 字段 | 单位 | 说明 |
|------|------|------|
| **#** | — | 工序序号 |
| **工序名** | — | 工序名称（可自定义） |
| **设备** | — | 执行该工序所使用的设备（下拉选择） |
| **工时 (H)** | 小时 | 该工序的标准加工时长 |
| **清洗 (H)** | 小时 | 工序完成后设备清洗所需时长；清洗期间设备不可用 |
| **AGV (H)** | 小时 | AGV 搬运所需时长 |
| **人员** | 人 | 该工序所需操作人员数，支持 ＋/－ 调整 |
| **原料A** | — | 该工序消耗的原料 A 用量 |
| **原料B** | — | 该工序消耗的原料 B 用量 |
| **脱模剂** | — | 该工序消耗的脱模剂用量 |

> **混合工序**：混合锅工序可标记为混合工序（`isMix`），用于工艺约束展示。

### 3.2 默认型号示例

系统预置 **A型、B型、C型** 三种型号，各 6 道工序（称量 → 预混 → 混合 → 模具装配 → 成型 → 整装），设备与物料配置可在界面中直接修改。

**A型（节选）**

| 工序 | 设备 | 工时(H) | 清洗(H) | 人员 | 原料A | 原料B | 脱模剂 |
|------|------|---------|---------|------|-------|-------|--------|
| 称量 | 称量台 | 0.5 | 0 | 1 | 25 | 10 | 0 |
| 混合 | 混合锅 | 3 | 1 | 1 | 0 | 0 | 0 |
| 成型 | 成型台 | 4 | 0 | 1 | 0 | 0 | 0 |

**B型 / C型**：结构相同，各工序工时与混合时长按型号独立配置（C型整体工时略短）。

### 3.3 型号管理操作

- **新增型号**：点击「+ 新增型号」，自动分配颜色与默认工序模板  
- **删除型号**：点击型号右侧「✕」（需确保无任务引用该型号）  
- **折叠 / 展开**：点击型号标题栏切换工序表显示  
- **工序排序**：通过上移按钮（▲）调整工序顺序  

---

## 4. 约束参数

**入口**：顶部 Tab → 「约束参数」

本模块分为三个区域：**工艺约束**（左上）、**班次 & 计划参数**（右上）、**值班表（按工作日）**（左侧工艺约束下方，跨列展示）。

### 4.1 工艺约束

| 参数 | 单位 | 说明 | 示例值 |
|------|------|------|--------|
| **原料A库存** | — | 当前原料 A 的可用库存量 | 200 |
| **原料B库存** | — | 当前原料 B 的可用库存量 | 80 |
| **脱模剂库存** | — | 当前脱模剂的可用库存量 | 10 |

> 生成排产前会校验各原料消耗总量；不足时弹窗提示并**阻止排产**。

### 4.2 值班表（按工作日）

人员约束由**值班表**驱动，替代原「可用人员总数」单一参数。机器可在班次时段内连续生产，但各时间段在岗人数不同，排产时工序所需人数不得超过该时段值班人数。

#### 4.2.1 配置方式

| 功能 | 说明 |
|------|------|
| **工作日 Tab** | 周一至周五（与排产天数一致）分别配置，每天可不同 |
| **时段行** | 每行：开始时间 / 结束时间 / 在岗人数 |
| **＋ 添加时段** | 在当前选中日追加一条时段 |
| **复制到全周** | 将当前日的值班表复制到其余工作日 |
| **摘要条** | 自动汇总，如 `08–12 · 2人 \| 12–14 · 1人 \| 14–18 · 2人` |

#### 4.2.2 默认示例（单日）

| 时段 | 在岗人数 |
|------|----------|
| 08:00 – 12:00 | 2 人 |
| 12:00 – 14:00 | 1 人 |
| 14:00 – 18:00 | 2 人 |

#### 4.2.3 空档与校验

| 类型 | 行为 |
|------|------|
| **时段空档** | 允许存在（如无 12:00–13:00 无人值班）；该时段**不可排产**，作为调度约束自动跳过，**不阻止**生成排产 |
| **重叠 / 无效时段** | 时段重叠、开始≥结束、超出班次范围、在岗人数＜1 等，页面红色提示；贪心算法生成排产前会拦截 |
| **空档提示** | 黄色提示条标注「XX:00–XX:00 无人在岗（该时段不可排产）」 |

> **业务含义**：产线机器 8:00–18:00 可连续运行；人员按值班表分段值守，空档仅表示该时段无可用人员，工序会自动推迟至有人时段或设备空闲时段。

### 4.3 班次 & 计划参数

| 参数 | 单位 | 说明 | 示例值 |
|------|------|------|--------|
| **班次开始时间** | 时（24H） | 每天机器开始生产的小时数 | 8 |
| **班次结束时间** | 时（24H） | 每天机器结束生产的小时数 | 18 |
| **排产天数** | 天 | 本次排产周期覆盖的工作日数量 | 5 |

**当前有效配置汇总**（页面自动计算）：

- 每日生产时间：10 小时（机器连续运行，**不停线**）  
- 班次：08:00 — 18:00  
- 值班表：按工作日逐日展示摘要  
- 混合后清洗：按各型号工序配置  

### 4.4 约束说明

| 约束 | 说明 |
|------|------|
| **工序清洗** | 清洗时间计入工序总占用时长，清洗期间设备不可用 |
| **班次边界** | 工序不跨日班次；当前班次剩余时间不足时，顺延至下一工作日班次开始 |
| **值班表人员** | 各时段在岗人数不同；工序所需人数不得超过该时段值班人数；跨时段工序须全程满足人员约束 |
| **设备独占** | 同一设备同一时刻仅一道工序 |

---

## 5. 排产结果

**入口**：顶部 Tab → 「排产结果」（有排产结果时 Tab 显示批次数角标）

### 5.1 结果摘要卡片

| 卡片 | 内容 |
|------|------|
| **调度算法** | 贪心 / HGNN+PPO |
| **完工时间** | 全部批次预计完工时刻（如：周四 10:00） |
| **批次总数** | 已排入计划的批次数 |
| **工序总数** | 生产工序道数（不含清洗展示项） |
| **班次工时** | 每日生产班次时长（如 10h/天，8:00–18:00 连续） |

### 5.2 动态重排状态横幅

执行动态重排后，结果区顶部显示黄色提示条：

- 重排基准时刻（NOW）  
- 已登记故障数量  
- 累计重排次数  
- 图例：🔒 = 已完成/进行中冻结 · 🟡虚线 = 新排工序  

### 5.3 设备利用率

以进度条展示各设备在排产周期内的占用比例（称量台、搅拌机、混合锅、成型台、整装区）。

### 5.4 视图切换

| 视图 | 说明 |
|------|------|
| **▦ 甘特图** | 按设备 × 时间轴可视化排程 |
| **📋 工单** | 表格列出每道工序的计划开始/完成、设备、人员等 |

**工具栏操作**：

- **📄 导出 PDF**：导出工单排程表（含算法、重排基准时刻等页眉信息）  
- **🔄 动态重排**：打开重调度弹窗（见第 7 节）  

**图例过滤**：按型号批次（如 A型×1批）及「🧹 清洗」标签筛选展示。

### 5.5 甘特图说明

- **横轴**：按工作日与班次内小时刻度（周一至周日，仅显示有效班次时段）  
- **纵轴**：设备行（仅显示本次排产涉及的设备）  
- **色块**：按型号颜色区分批次工序；灰色块为清洗  
- **红色 NOW 竖线**：动态重排的基准时刻  
- **红色斜纹区域**：设备故障窗口（重排时登记）  
- **样式**：冻结工序半透明 + 🔒；新排工序黄色虚线边框  

### 5.6 工单表说明

| 列 | 说明 |
|----|------|
| 状态 | 已完成 / 进行中 / 新排 / 待排 |
| 工单号 | WO-001 格式；与计划任务绑定，插单后已有批次工单号保持不变 |
| 批次、工序、设备 | 排程明细 |
| 计划开始 / 完成 | 班次时间轴上的时刻 |
| 工时、人员、备注 | 来自型号与任务配置 |

### 5.7 重排历史

每次动态重排后，结果页底部「重排历史」记录：

- 执行时间、基准时刻  
- 冻结工序数、新排工序数  
- 本次新增故障数、插单数  

---

## 6. 调度算法

系统支持两种排产算法，可在顶部下拉菜单切换。**两种算法均按值班表约束人员**，并在 8:00–18:00 班次内连续排产（无午休停线）。

| 算法 | 运行方式 | 说明 |
|------|----------|------|
| **贪心算法**（默认） | 纯前端 | 按任务优先级逐批安排工序，在班次 / 设备 / 值班表人员约束下选取最早可用时间槽；速度快，无需后端 |
| **HGNN+PPO 智能调度** | 前端 + 后端 API | 调用 `POST /api/schedule/hgnn-ppo`，HGNN+PPO 求解工序排序后映射到班次时间轴，映射阶段同样校验值班表人员约束；需启动 `python server.py` |

### 6.1 物料校验（两种算法共用）

生成排产前汇总计划内各原料消耗，与库存对比；不足则弹窗列出缺口并中止排产。

### 6.2 值班表校验（贪心算法）

贪心算法在生成排产前校验值班表配置（重叠、无效时段等）；空档不拦截。HGNN 后端接收相同 `dutyRosterByDay` 结构，在槽位映射时执行人员约束。

### 6.3 HGNN+PPO 使用说明

1. 安装依赖：`pip install -r requirements.txt`  
2. 启动后端：`python server.py`（默认 `http://localhost:8000`）  
3. 前端开发模式下 Vite 已将 `/api` 代理至后端  
4. 选择「HGNN+PPO 智能调度」后点击「生成排产」；求解过程按钮显示「⏳ 求解中...」  

求解失败时提示检查后端是否启动。

---

## 7. 动态重调度

**入口**：排产结果页 → 「🔄 动态重排」

用于应对**插单、设备故障、产线中断**等突发情况，在保留已完成 / 正常进行中工序的前提下，重排尚未完成的工序。

### 7.1 重调度弹窗配置

| 配置项 | 说明 |
|--------|------|
| **重调度基准时刻 (NOW)** | 格式 `周X HH:MM`（如 `周二 09:00`）；该时刻之前已完成的工序冻结 |
| **设备故障窗口** | 可选；指定设备、起止时间、原因；进行中工序若与故障重叠则作废并从该工序重排 |
| **新增插单任务** | 可选；设置优先级（0=最高）、型号、批次、备注 |
| **影响预估** | 实时统计：已完成(冻结)、进行中(冻结)、待重排、故障作废数量 |

### 7.2 冻结与重排规则

| 原工序状态（相对 NOW） | 处理方式 |
|------------------------|----------|
| 已结束（end ≤ NOW） | 冻结，不重排 |
| 进行中且未被故障打断 | 冻结，允许自然完成 |
| 进行中且被故障打断 | 作废，从该工序起重排 |
| 未开始 | 参与重排 |

### 7.3 插单与工单号

动态重排支持在弹窗中**新增插单任务**（优先级、型号、批次、备注）。插单执行后：

| 规则 | 说明 |
|------|------|
| **已有批次** | 保留原工单号（如 WO-001、WO-002），与计划任务 `taskId` 绑定 |
| **插单批次** | 分配新的工单号（如 WO-003），不占用已有编号 |
| **批次进度** | 以 `taskId + 批次序号` 追踪各批次工序进度，避免同型号多批次时进度串扰 |
| **计划合并** | 插单任务确认后写入「本周计划」，后续重排可继续叠加 |

> 修改约束参数或重新「生成排产」后，建议以最新排产结果为基准再做动态重排，以确保工单与任务正确关联。

### 7.4 算法一致性

| 当前选择的调度算法 | 动态重排实现 |
|------------------|--------------|
| 贪心算法 | 前端 `runSchedule`（冻结时间线、故障窗、值班表人员、批次进度） |
| HGNN+PPO | 后端 `POST /api/schedule/hgnn-ppo/reschedule`（冻结 + HGNN 求解 + 值班表槽位映射） |

重排完成后更新甘特图、工单表，并累计重排历史；PDF 导出页眉会标注动态重排基准时刻。

---

## 8. 智能客服助手

> **当前状态：已实现，界面默认隐藏**（待汇报演示时启用）

### 8.1 功能说明

- 页面右下角悬浮球，点击展开智能对话框  
- 红色/蓝色主题与主站 Design System 对齐（`#2563eb` 主色、`#f1f5f9` 背景）  
- 内置排产系统 FAQ（生成排产、算法对比、动态重排、甘特图图例等）  
- 支持关键词匹配回复；可传入当前算法、Tab、是否已有排产结果等上下文  
- 组件已模块化，便于后续对接真实 LLM API  

### 8.2 代码位置

```
src/components/agent/
├── AgentWidget.jsx      # 入口（FAB + 面板）
├── AgentFab.jsx         # 右下角悬浮球
├── AgentPanel.jsx       # 对话框容器
├── AgentHeader.jsx      # 标题栏
├── AgentFAQList.jsx     # 常见问题列表
├── AgentMessageList.jsx
├── AgentInput.jsx
├── useAgentChat.js      # 对话状态 Hook
├── agentKnowledge.js    # FAQ 与规则回复
├── agentTheme.js        # 主题常量
└── index.js             # 统一导出
```

### 8.3 启用方式（汇报演示时）

在 `src/App.jsx` 中取消以下两处注释即可显示：

```javascript
// 第 2 行附近 — 恢复 import
import { AgentWidget } from "./components/agent";

// 第 1734 行附近 — 恢复组件渲染
<AgentWidget context={{ algo, tab, hasEvents: events.length > 0 }} />
```

**自定义 AI 接口示例**：

```jsx
<AgentWidget
  context={{ algo, tab, hasEvents: events.length > 0 }}
  onSend={async (text, ctx) => {
    const res = await fetch("/api/chat", { method: "POST", body: JSON.stringify({ text, ctx }) });
    const data = await res.json();
    return data.reply;
  }}
/>
```

---

## 9. 附：系统设备清单

| 设备名称 | 对应工序 |
|---------|---------|
| 称量台 | 称量 |
| 搅拌机 | 预混 |
| 混合锅 | 混合 |
| 成型台 | 模具装配、成型 |
| 整装区 | 整装 |

---

## 10. 附：部署与运行

### 10.1 前端

```bash
npm install
npm run dev      # 开发 http://localhost:5173
npm run build    # 构建 dist
```

### 10.2 后端（HGNN+PPO / 智能动态重排）

```bash
pip install -r requirements.txt
python server.py   # http://localhost:8000
```

| API | 说明 |
|-----|------|
| `POST /api/schedule/hgnn-ppo` | 首次智能排产 |
| `POST /api/schedule/hgnn-ppo/reschedule` | 智能动态重排 |

### 10.3 项目结构（核心）

| 路径 | 说明 |
|------|------|
| `src/App.jsx` | 主应用（计划、型号、约束、结果、重排弹窗） |
| `src/components/agent/` | 智能客服组件（默认未挂载） |
| `server.py` | FastAPI 调度 API |
| `scheduler_hgnn.py` | HGNN+PPO 求解器 |
| `vite.config.js` | 开发代理 `/api` → `:8000` |

---

*文档版本：v4.1 · 与当前系统功能同步（值班表按工作日配置、HGNN 人员约束同步、插单工单号稳定、8:00–18:00 连续生产）*
