# 排产系统意见评估

---

## 1. BOM 层级（多级子物料、替代料）

**观点：** 本来这个系统为了兼容agv固定调度就是弱化物料这块，属 ERP/MRP 范畴，与排产调度定位不同。系统可以引入物料多样性，后面若有实际数据可接入。

---

## 2. 工时浮动系数（批次大小、班组熟练度）

**观点：** 值班表按姓名统计人数，无技能/熟练度维度，这里每个工序时长固定，如果根据每个人熟练度规定工序时长有点乱。

---

## 3. 自定义优化权重

**观点：**
- **均衡设备负载 / 降低换型清洗**：设备负载目标粒度太小了，实现了一下，不明显。这块很模糊，换型清洗是固定在混合工序这一块中。
- **人工成本最低 / 制造成本最优**：无单价与成本模型，不好评估成本，感觉最短完工时间就能保证一定的成本。

---

## 4. RL 参数可配置

**观点：** 后端已有 `episodes` 等参数接口，双算法（贪心 / HGNN）可同计划对比。可以实现：展示调优后默认 episodes，可选高级调节。

---

## 5. 工序替代设备（故障切备用机）

**观点：** 当前每道工序绑定唯一设备类型，每类型仅 1 条时间线。动态重排的设计即「故障后再排产」，初排内置主备自动切换会削弱该演示价值。可以实现：若需「同类型多台」，宜走约束参数「设备台数」扩展并行产能。

---

## 6. 工装 / 模具绑定约束
**观点：** 系统中不用考略模具实体，只有模具装配这个工序。模具装配仅为工序名称，设备为成型台，与模具占用无关。

---
