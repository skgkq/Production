# 日/周排产系统 · 增强需求修改调研报告

> **文档性质**：需求可行性调研 / 修改评估  
> **系统版本**：v4.1（贪心 + HGNN+PPO、值班表人员约束、动态重排）  
> **评估原则**：与现有系统定位一致、改动可落地、不勉强堆 ERP 能力  

---

## 1. 评估背景

本系统定位为**一号线日/周生产排产调度**，核心能力是：在班次、设备独占、值班表人员、物料库存等约束下，为各型号批次生成工序时间轴，并支持插单与设备故障下的动态重排。

近期提出的 6 类增强需求，经对照 [`日周排产系统功能说明.md`](日周排产系统功能说明.md) 与 [`src/App.jsx`](src/App.jsx)、[`server.py`](server.py)、[`scheduler_hgnn.py`](scheduler_hgnn.py) 实现，结论如下：

| 序号 | 需求 | 结论 | 优先级建议 |
|------|------|------|------------|
| 1 | BOM 层级（多级子物料、替代料） | **不做** | — |
| 2 | 工时浮动系数（批次大小、班组熟练度） | **不做** | — |
| 3 | 自定义优化权重 | **部分可做**（仅贪心、且偏前两项） | 低 |
| 4 | RL 参数可配置 + 多算法对比报表 | **可做** | 中 |
| 5 | 工序替代设备（故障切备用机） | **不做**（多台同类型见 §3.5 扩展说明） | — |
| 6 | 工装 / 模具绑定约束 | **不做** | — |

---

## 2. 系统现状摘要（评估基准）

### 2.1 调度与数据模型

- **排产粒度**：任务（型号 + 批次数 + 优先级）→ 线性工序链 → 设备时间槽。
- **设备**：固定 5 类（称量台、搅拌机、混合锅、成型台、整装区），每道工序绑定**唯一** `eq` 字段。
- **物料**：工序级扁平字段 `matA` / `matB` / `release`，排产前做**三种原料总量**与库存对比，不足则拦截；无 BOM 树、无替代料、无齐套展开。
- **AGV**：以工序字段 `agv`（小时）形式计入总占用时长，属于**固定时序参数**，不参与物料决策或路径优化。
- **人员**：值班表按工作日配置时段与姓名列表，人数由姓名数量统计；用于容量约束，**不**按个人技能排班。
- **算法**：贪心（前端最早可行槽）+ HGNN+PPO（后端 FJSP，终局奖励 `reward = -makespan`）。
- **动态重排**：登记**指定设备**故障时间窗 → 冻结/作废受影响工序 → 重排；这是演示「突发再排产」能力的设计入口。

### 2.2 设计取向说明

本系统在架构上**有意弱化物料与资源细粒度管理**，以兼容 AGV 固定调度、固定工艺路线、固定设备清单的一号线 Demo 场景。物料仅保留「够不够用」的总量校验，而非 MRP/ERP 级物料计划。后续评估均在此前提下进行。

---

## 3. 分项评估

### 3.1 BOM 层级：多级子物料、替代料

**结论：不做。**

#### 需求含义

BOM（Bill of Materials）层级指产品按「成品 → 半成品 → 零件/原料」树形展开，并支持子物料用量累计、缺料时按规则选用替代料。属于 **MRP/ERP 物料管理** 范畴，与「排产调度」是不同产品层。

#### 与现有系统的冲突（致命原因）

1. **数据模型不匹配**  
   当前型号配置为固定工序表，物料挂在工序上的三个数字字段；排产逻辑仅执行 `needA += op.matA × batches` 式汇总。不存在父项/子项、用量展开、替代组、齐套率等结构，引入 BOM 等于重建物料子系统。

2. **与系统定位相悖：为 AGV 固定调度而弱化物料**  
   一号线场景下，AGV 搬运以工序 `agv` 固定时长并入排程，工艺路线与设备绑定预先确定。系统关注的是**时间轴与资源占用**，而非「因 BOM 变化导致工艺分叉」。做多级 BOM 会牵引出：按子件缺料改工艺、按替代料换路线等问题，超出当前调度边界。

3. **替代料逻辑无业务锚点**  
   替代料需定义：替代组、优先级、换算比例、库存扣减规则、是否影响工序参数等。现有库存只有三种原料标量，无法支撑「B 不够换 B'」类决策，硬做只能是空壳配置，无法参与真实排产。

4. **投入产出比极低**  
   需 BOM 编辑器、展开引擎、与排产/库存联动、大量 UI 与迁移；对演示「日周排产 + 动态重排 + HGNN」几乎无增益，却显著拉高复杂度与维护成本。

#### 若业务方坚持要「多一点物料」

最多可考虑：在工序上扩展更多扁平原料种类（仍无层级、无替代），价值有限，**不建议**作为本期目标。

---

### 3.2 工时浮动系数：批次大小、班组熟练度

**结论：不做。硬做属于「为了做而做」，与值班表设计不匹配。**

#### 需求含义

根据批次规模、班组熟练度对标准工时 `dur` 乘以系数，使同一工序在不同条件下加工时长不同。

#### 致命原因

1. **「批次」语义与「批次大小」不一致**  
   系统中 `batches` 表示生产**锅数/批次数**，不是单批物理批量（如每锅 500kg）。若系数依赖「批次大小」，需新增独立字段与业务定义，否则只能勉强用批次数代替，缺乏制造含义。

2. **值班表无熟练度维度**  
   v4.1 值班表已改为**按姓名统计人数**（小明、小红等），用于回答「该时段有几个人在岗」，而非「谁具备何种技能、熟练度多少」。人员约束是 `工序所需人数 ≤ 时段在岗人数`，**没有**人员-工序-技能矩阵。  
   若引入熟练度，需另建：班组档案、技能等级、与值班姓名的绑定、各等级系数表——与当前「姓名即人数」模型重复且冲突。

3. **与 HGNN 训练链路耦合弱、收益难验证**  
   系数只影响输入加工时间，不改变算法框架；Demo 数据下微调系数对 makespan 的影响难以向业务方解释，演示价值低。

4. **硬做的典型表现**  
   全局加一个「工时系数 1.05」滑条：技术上几行代码，但无法体现「批次大小 + 班组熟练度」的真实需求，汇报时容易被追问数据来源，反而暴露设计空洞。

---

### 3.3 自定义优化权重

**结论：部分可做——贪心算法可适度扩展；HGNN 侧多项权重基本不符合；整体缺少成本模型。**

#### 需求含义

用户可选择优化倾向，例如：最短完工、均衡设备负载、降低换型清洗成本、人工成本最低等。

#### 分项判断

| 优化目标 | 贪心 | HGNN+PPO | 说明 |
|----------|------|----------|------|
| 优先最短完工 | 已接近 | 已优化 makespan | 默认行为，无需权重 |
| 均衡设备负载 | **可尝试** | 难 | 贪心可在选槽时加入负载启发式 |
| 降低换型/清洗成本 | **可尝试** | 难 | `cleanDur` 已存在，可加权计入评分 |
| 人工成本最低 | **弱** | 难 | 无人工单价、无成本模型 |

#### 致命原因（HGNN 与后几项）

1. **PPO 奖励函数单一**  
   [`scheduler_hgnn.py`](scheduler_hgnn.py) 中终局奖励固定为 `reward = -makespan`，中间步奖励为 0。前端滑条切换「均衡负载 / 降清洗 / 降人工」**无法映射**到已训练策略；改奖励需重新设计 MDP、调参甚至重训，不是「配置项」能解决的。

2. **无制造成本模型**  
   系统未定义：原料单价、人工小时费率、设备折旧、换型单次成本。  
   「人工成本最低」「制造成本对比」缺乏计算基础，最多用代理指标（如总清洗时长、总人工占用小时），与业务理解的「成本」差距大，**后几项权重偏弱、说服力不足**。

3. **贪心多目标本质仍是启发式**  
   可在 `findSlot` 或批次排序层加加权评分（如 `α×完工推迟 + β×设备负载方差 + γ×清洗时长`），但属于规则叠加，**不能保证 Pareto 最优**，演示时需说明「仅贪心、仅近似」。

#### 建议范围（若要做）

- 仅在前端贪心路径增加 1～2 种「优化模式」预设（如：默认最早完工 / 倾向减少清洗总时长）。
- HGNN 保持 makespan 单目标，界面注明不支持多目标权重。
- **不承诺**「人工成本最低」「制造成本最优」类表述。

---

### 3.4 强化学习参数可配置 + 多算法对比报表

**结论：可做。**

#### 需求含义

- 暴露 HGNN+PPO 训练相关参数（如 episodes、学习率等）供调整或展示。  
- 对贪心与 HGNN 在同一计划下输出对比报表：完工时长、设备利用率等。

#### 可行性依据

1. **后端已具备参数接口**  
   [`solve_fjsp`](scheduler_hgnn.py) 支持 `n_episodes, lr, gamma, eps_clip, entropy_coef, d`；[`server.py`](server.py) 的 `ScheduleRequest` 已含 `episodes`，前端目前写死 `300`。

2. **双算法已并存**  
   同一 `plan / types / cst` 可分别跑贪心与 HGNN，对比 makespan、工序数、设备利用率（现有 `eqUtil` 逻辑）具备数据基础。

3. **对比报表可渐进增强**  
   - 可直接对比：完工时刻、总清洗时长、各设备利用率、重排后变化。  
   - 需修正或说明：当前利用率分母为全局 makespan，与「日历可用工时」定义不同时需统一口径。  
   - 制造成本、在制品工时、产能利用率等需先定义模型（见 3.3），可列为「代理指标」或二期。

#### 实施注意（与用户判断一致）

- **生产/汇报场景通常提供调优后的默认参数**，而非要求用户每次手调 RL 超参。  
- UI 上更适合：**展示当前采用的推荐参数** + 可选「高级：episodes」；对比报表侧重 **贪心 vs HGNN** 的业务可读指标，而非 PPO 训练曲线。

#### 建议交付范围

| 项 | 建议 |
|----|------|
| episodes 等 1～2 项入约束参数或算法设置 | 是 |
| 一键双算法对比表（makespan、利用率、清洗合计） | 是 |
| 暴露全部 RL 超参供用户随意调 | 否（易误导） |
| 制造成本/WIP 完整量化 | 否（缺模型） |

---

### 3.5 工序替代设备（搅拌机故障切备用机）

**结论：不做「工序级备用机 / 自动切换」。若未来要表达「同类型多台设备」，应走约束参数里的**设备数量**配置，与本期「故障后再排产」叙事一致，而非工序级替代列表。**

#### 需求含义

某工序可配置多台候选设备（如搅拌机 A / 搅拌机 B），主设备故障时自动切备用机继续排产。

#### 概念澄清：设备类型 vs 设备台数

| 维度 | 现状 | 说明 |
|------|------|------|
| 工序绑定 | 每道工序对应一个**设备类型**（`op.eq`：称量台、搅拌机、混合锅…） | 这是「去哪类设备加工」，不是物理机编号 |
| 排产时间线 | 每种类型目前只有 **1 条**时间线 `tls[eq]` | 同一类型上的工序串行占用，等价于「该类型仅 1 台」 |
| 故障登记 | 按**设备类型名**屏蔽时间段 | 如「搅拌机 周二 9:00–13:00 故障」 |

因此：工序表是「类型 → 工序」的 1:1；**排产引擎尚未支持「同类型多台并行」**。若业务上要「2 台搅拌机同时干活」，更合理的扩展是：在**约束参数**中增加「各设备类型可用台数」（如 `eqCount: { 搅拌机: 2, 混合锅: 1, … }`），调度时为该类型维护多条并行时间线或在 `findSlot` 中选最早空闲的「虚拟机位」——这是**产能配置**，不是「工序备用机列表」。

#### 致命原因（为何仍不做「工序级替代设备」）

1. **与现有扩展路径重复且更复杂**  
   「工序配置 altEq 备用搅拌机」要求每道工序维护候选列表、各机工时/清洗可能不同，UI 与 HGNN 的 `proc[j][o]` 多机矩阵都要改。  
   而一号线 Demo 的真实诉求往往是「某类设备有多台」——用**全局台数**即可覆盖，无需工序级备用机字段。

2. **动态重排的设计目的就是「故障后再排产」**  
   系统已提供：登记某**设备类型**在某时段不可用 → 作废受影响在制工序 → 冻结未冲突工序 → 重算时间轴。这是演示 **突发不可用时的滚动重调度** 的核心场景。  
   若在**初排**就内置「主备自动切换、用户无感」，故障不再触发有意义的重排演示，与产品故事线冲突。  
   更合理的分工是：初排按「各类型台数」正常并行；**某一台（或该类型部分产能）故障**时，仍走动态重排——若该类型 `count ≥ 2`，重排自然会把任务挤到同类型剩余台位，无需单独做「备用机字段」。

3. **HGNN 多机能力与当前建模未接通**  
   FJSP 环境支持 `proc[j][o] = {m1: t, m2: t}`，但 [`build_proc_jobs`](server.py) 按工序唯一 `eq` 只写入一个机器索引；设备索引来自全厂 `eq_list` 去重，**不是**「搅拌机-1 / 搅拌机-2」实例。工序级备用机需重构设备索引与故障语义（故障的是哪一台），Demo 设备清单下收益有限。

4. **故障 UI 与「备用机」语义易混淆**  
   当前故障窗按类型名屏蔽。若同时存在「工序备用机」与「类型台数」，业务上要解释：故障的是类型下某一台还是整类——在缺少物理机编号的前提下，体验混乱。

#### 若后续确有「多台同类型」需求（本期仍不做，仅作方向说明）

- **建议做法**：约束参数 Tab 增加「设备台数」表（默认均为 1），调度层为 `count > 1` 的类型扩展并行时间线；甘特图可展示为「搅拌机 #1 / #2」行。  
- **仍不建议**：工序级 `altEq` + 初排自动 failover（削弱动态重排演示价值）。  
- **与本期关系**：属于独立小功能，**不等同**于原需求的「备用搅拌机配置」，未纳入本期建议范围。

---

### 3.6 工装 / 模具绑定约束

**结论：不做。系统中不存在「模具」这一业务实体。**

#### 需求含义

模具作为有限资源，在某占用时段内绑定特定工单/型号，占用期间其他工单不可使用该模具。

#### 致命原因

1. **无模具主数据与资源维度**  
   全代码库无 `mold` / `模具` 实体、编号、数量、适用型号等配置。工序名「模具装配」只是**工艺步骤名称**，执行设备为「成型台」，与模具资源占用无关。

2. **调度引擎仅两类资源时间线**  
   - 设备时间线 `tls[eq]`：同一设备互斥。  
   - 人员时间线 `workerTl`：时段人数容量。  
   **没有**第三类「模具时间线」。增加模具约束需：模具档案、批次-模具绑定、选槽时校验模具空闲、重排时模具冻结规则——相当于新增一整条资源调度链。

3. **与型号/批次模型未挂钩**  
   计划任务只有型号、批次数、优先级，未指定「本批用哪套模具」。要做约束必须先定义绑定规则（按型号默认模具？按批指定？），当前无任何入口。

4. **脱模剂 ≠ 模具**  
   `release`（脱模剂）仅为消耗型物料字段，参与库存总量校验，**不代表**模具物理占用或模具寿命。

5. **投入与一号线 Demo 范围不符**  
   模具管理通常归属 MES/工装管理系统，与日周排产解耦。在本系统中硬加，易做成「只有一个全局互斥锁」的伪功能，无法通过业务评审。

---

## 4. 总结与建议

### 4.1 不建议纳入本期

| 需求 | 一句话原因 |
|------|------------|
| BOM 层级 | 为 AGV 固定调度有意弱化物料；无 BOM 树与替代料模型，属 ERP 范畴 |
| 工时浮动系数 | 批次数≠批量；值班表只有人数无熟练度，硬做无业务依据 |
| 工序替代设备 | 工序级备用机不做；同类型多台宜走约束参数「设备台数」，故障仍靠动态重排 |
| 模具绑定 | 无模具实体与资源调度维度，「模具装配」仅为工序名 |

### 4.2 可纳入本期（从简）

| 需求 | 建议范围 |
|------|----------|
| 自定义优化权重 | **仅贪心**：1～2 种模式（最早完工 / 略偏少清洗）；HGNN 不改奖励 |
| RL 参数 + 算法对比 | 展示**调优后默认 episodes**；一键贪心 vs HGNN 对比表（makespan、利用率、清洗合计） |

### 4.3 架构边界（供汇报引用）

> 本系统是一号线**日/周排产与动态重排**演示平台，重点在班次、人员值班表、设备独占、插单与故障重排，以及贪心与 HGNN 算法对比。  
> **物料、BOM、模具、多机路由、成本模型** 等 ERP/MES 深度能力不在本期范围；勉强接入会稀释核心演示、增加不可验证的配置项。

---

## 5. 附录：关键代码锚点

| 能力 | 位置 |
|------|------|
| 扁平物料校验 | `src/App.jsx` — `generate()` 中 `needA/needB/needR` |
| 工序单机绑定 | `op.eq`；`server.py` — `build_proc_jobs` |
| 故障重排 | `src/App.jsx` — `reschedule()`、`failures` |
| HGNN 单目标奖励 | `scheduler_hgnn.py` — `reward = -makespan` |
| 固定设备清单 | `src/App.jsx` — `EQ_ORDER` |
| HGNN episodes | `src/App.jsx` 写死 `300`；`server.py` — `ScheduleRequest.episodes` |

---

*文档版本：v1.0 · 与系统功能说明 v4.1 对齐*
